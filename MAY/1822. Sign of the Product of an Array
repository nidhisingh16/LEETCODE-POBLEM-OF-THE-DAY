class Solution:
    def arraySign(self, nums: List[int]) -> int:
        prod=1
        for ele in nums:
            prod*=ele
        if prod>0:
            return 1
        if prod<0:
            return -1
        return 0


# Time complexity: O(n)
# Space complexity: O(1)
